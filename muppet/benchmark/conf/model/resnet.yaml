_target_: muppet.benchmark.models.classifier.Classifier
name: resnet18
model:
    # load from torchvision
    _target_: torchvision.models.get_model
    name: resnet18
    weights: IMAGENET1K_V1

    # example of loading from torch.hub
    # _func_: torch.hub.load
    # repo_or_dir: pytorch/vision
    # model: resnet18
    # weights: IMAGENET1K_V1

    # example of loading from local file
    # _func_: torch.load
    # f: resnet18.pt
pretrained: True
random_state: 105
